import React from 'react';
import ReactDOM from 'react-dom';
import PizzaPlanetApp from './components';

const App = (
  // <React.StrictMode>
  <PizzaPlanetApp />
  // </React.StrictMode>
);

ReactDOM.render(App, document.getElementById('root'));
